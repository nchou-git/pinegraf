"""Candidate generation for entity resolution belongs here.

Future ML-based blocking should produce conservative candidate entity sets for
human-reviewed merges. The deterministic resolver remains the authoritative
write path until those candidates are explicitly accepted.
"""
